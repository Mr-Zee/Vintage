# Watch & Bangle Store (React + Tailwind)

Pages:
- Home (`/`)
- Products (`/products`) — filter sidebar, sort dropdown, grid/list toggle
- About (`/about`)
- Contact (`/contact`)
- Sticky Navbar (fixed at top)

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
